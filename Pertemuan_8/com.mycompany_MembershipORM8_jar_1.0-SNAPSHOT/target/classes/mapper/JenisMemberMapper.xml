<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.JenisMemberMapper">
    
<!--    <insert id="insert" parameterType="model.JenisMember">
        INSERT INTO jenis_member (id, nama)
        VALUES (#{id}, #{nama})
    </insert>
    
    <update id="update" parameterType="model.JenisMember">
        UPDATE jenis_member
        SET nama = #{nama}
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="model.JenisMember">
        DELETE FROM jenis_member
        WHERE id = #{id}
    </delete>
    
    <select id="findAll" resultType="model.JenisMember">
        SELECT * FROM jenis_member
    </select>-->

</mapper>
